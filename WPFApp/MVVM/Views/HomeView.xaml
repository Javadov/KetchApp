<UserControl x:Class="WPFApp.MVVM.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFApp.MVVM.Views" xmlns:viewmodels="clr-namespace:WPFApp.MVVM.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFFCF9F9">
        <TextBlock Text="Välkommen till Adressboken" Foreground="#FFCD1717" Margin="200 100 200 0" TextAlignment="Center"/>
        <Button d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}" Command="{Binding GoToAddContactCommand}" Content="Add" Width="100" Height="30" Margin="100 100 300 0"/>
        <Button d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}" Command="{Binding GoToContactsCommand}" Content="List" Width="100" Height="30" Margin="300 100 100 0"/>
    </Grid>
</UserControl>
